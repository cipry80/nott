// Base URL can be something like 'http://localhost:3000'
const API_BASE_URL = '';

const Constants = {};

Constants.API_BASE_URL = API_BASE_URL;

// An example of a possible app URL
// Constants.LOGIN_URL = `${API_BASE_URL}/login`;

export default Constants;
